{
  "tasks": [
    {
      "args": ["-m", "src.cosimlex", "-f"],
      "command": "${command:python.interpreterPath}",
      "group": {
        "kind": "none"
      },
      "label": "convert",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["-m", "src.subtask1"],
      "command": "${command:python.interpreterPath}",
      "group": {
        "kind": "none"
      },
      "label": "subtask1",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["-m", "src.cv"],
      "command": "${command:python.interpreterPath}",
      "group": {
        "kind": "none"
      },
      "label": "subtask1 cv",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["-m", "src.stats_best"],
      "command": "${command:python.interpreterPath}",
      "group": {
        "kind": "none"
      },
      "label": "subtask1 stats",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    },
    {
      "args": ["-m", "src.utils"],
      "command": "${command:python.interpreterPath}",
      "group": {
        "kind": "none"
      },
      "label": "subtask1 utils",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "type": "shell"
    }
  ],
  "version": "2.0.0"
}
